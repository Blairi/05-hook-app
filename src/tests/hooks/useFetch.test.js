// import { renderHook } from "@testing-library/react-hooks";
// import { useFetch } from "../../hooks/useFetch";

describe('Pruebas en useFetch', () => {
    test('should first', () => {  });

    // test('Debe de retornar la infomración por defecto', () => { 

    //     const { result } = renderHook(() => { useFetch('https://www.breakingbadapi.com/api/quotes/2') });

        
    //     setTimeout(() => {
            
    //         const { data, loading, error } = result.current;

    //         expect( data ).toBe(null);
    //         expect( loading ).toBe(true);
    //         expect( error ).toBe(null);
            
    //     }, 3000);

    // });

    // test('Debe de tener la info deseada', async () => {
    //     const { result, waitForNextUpdate } = renderHook(() => { useFetch('https://www.breakingbadapi.com/api/quotes/2') });

    //     setTimeout(() => {
            
    //         const { data, loading, error } = result.current;

    //         expect( data ).toBe(null);
    //         expect( loading ).toBe(true);
    //         expect( error ).toBe(null);
            
    //     }, 3000);

    // });

});